# DNS-002 多様な名前へのNXDOMAIN集中

15分で50種類以上の名前解決がNXDOMAINになり、全問い合わせの80%以上。

|項目|内容|
|---|---|
|対象ログ|DNS|
|必要フィールド|`_time`, `log_type`, `src_ip`, `dns_query`, `rcode`|
|ATT&CK 戦術|[TA0011 Command and Control](https://attack.mitre.org/tactics/TA0011)|
|技法 → サブ技法|[T1568 Dynamic Resolution](https://attack.mitre.org/techniques/T1568) → [T1568.002 Domain Generation Algorithms](https://attack.mitre.org/techniques/T1568/002)|
|検索|15分窓／5分間隔／5分遅延|
|工夫|単なる失敗数から、名前の種類数と失敗比率を組み合わせる。|

rcodeは文字列NXDOMAIN等。数値コードは正規化する。

## SPL

```spl
index=detection_lab log_type="dns" earliest=-20m@m latest=-5m@m
| eval query=lower(rtrim(dns_query,".")), reply=upper(rcode)
| where NOT match(query,"[.]arpa$")
| stats count as queries
        sum(eval(if(reply="NXDOMAIN",1,0))) as failed
        dc(eval(if(reply="NXDOMAIN",query,null()))) as failed_names
        min(_time) as first_seen
        max(_time) as last_seen
    by src_ip
| where failed_names >= 50 AND failed/queries >= 0.8
| eval rule_id="DNS-002", attack_id="T1568.002"
```

**範囲：** DGAの候補で、DNS設定不備でも一致。リゾルバ集約後のIPでは端末を識別できない。

## 参考文献

- [T1568.002 Domain Generation Algorithms](https://attack.mitre.org/techniques/T1568/002)：対象行動と戦術の対応を参照。
- [Excessive DNS Failures](https://github.com/splunk/security_content/blob/4cd62e811c5eddf1245c780d48823d7b38998566/detections/network/excessive_dns_failures.yml)：DNS失敗の集中から不審な名前解決を探す。
