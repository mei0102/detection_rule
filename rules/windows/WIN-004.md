# WIN-004 ローカルAdministratorsへのメンバー追加

組み込みAdministratorsグループへのメンバー追加を検知。

|項目|内容|
|---|---|
|対象ログ|Windows Event|
|必要フィールド|`_time`, `channel`, `event_id`, `host`, `log_type`, `target_sid`|
|ATT&CK 戦術|[TA0003 Persistence](https://attack.mitre.org/tactics/TA0003), [TA0004 Privilege Escalation](https://attack.mitre.org/tactics/TA0004)|
|技法 → サブ技法|[T1098 Account Manipulation](https://attack.mitre.org/techniques/T1098) → [T1098.007 Additional Local or Domain Groups](https://attack.mitre.org/techniques/T1098/007)|

## SPL

```spl
index=detection_lab log_type="windows" earliest=-10m@m latest=-5m@m
| where isnotnull(_time)
    AND isnotnull(channel)
    AND isnotnull(event_id)
    AND isnotnull(host)
    AND isnotnull(log_type)
    AND isnotnull(target_sid)
    AND channel="Security"
    AND event_id=4732
    AND target_sid="S-1-5-32-544"
| stats count as observed min(_time) as first_seen max(_time) as last_seen by host
| where observed >= 1
| eval rule_id="WIN-004", attack_id="T1098.007"
```

**範囲：** 他の特権グループへの追加は対象外。

## 参考文献

- [MITRE ATT&CK — T1098.007 Additional Local or Domain Groups](https://attack.mitre.org/techniques/T1098/007)：既存アカウントへのローカル／ドメイングループ追加と、継続アクセス・権限取得への利用を説明する技法ページ。
- [Microsoft Learn — 4732(S): A member was added to a security-enabled local group.](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4732)：ローカルセキュリティグループへの追加イベントを説明。
